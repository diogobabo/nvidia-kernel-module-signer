# nvidia-kernel-module-signer
A simple, automated Bash script to sign NVIDIA kernel modules for Secure Boot on Ubuntu and similar Linux systems. Supports automatic detection of NVIDIA driver versions, signing of compressed and uncompressed kernel modules, DKMS integration, and MOK enrollment to enable Secure Boot compatibility for NVIDIA GPUs.
